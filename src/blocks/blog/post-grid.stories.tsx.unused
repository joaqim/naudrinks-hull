import { Meta, Story } from '@storybook/react'
import PostGrid from './post-grid'
import { DefaultCard } from './post-card.stories'

export default { component: PostGrid, title: 'Post Grid' } as Meta

export const DefaultGrid: Story = ({ posts }) => {
  return <PostGrid posts={posts}></PostGrid>
}

DefaultGrid.args = { posts: [{ ...DefaultCard.args }] }
